report_status_checks: true
rules:
  - name: "avoid short variable names"
    applicable_files:
      - "*.cs"
    description: "Avoid using shorthand variable names like val instead of value. The full name is always preferred."

language: "en"
